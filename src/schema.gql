# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

# create a new freight to organization
input CreateFreightInput {
  destination_city: String!
  have_insurance: Boolean!
  height: Int!
  is_express: Boolean!
  length: Int!
  organization_id: Int!
  origin_city: String!
  package_id: Int!
  quantity: Int!
  risk: RiskDefinition = low
  shipping_method: ShippingMethod!
  weight: Int!
  width: Int!
}

type Freight {
  createdAt: String!
  destination_city: String!
  have_insurance: Boolean!
  height: Int!
  id: ID!
  is_express: Boolean!
  length: Int!
  organization_id: Int!
  origin_city: String!
  package_id: Int!
  quantity: Int!
  risk: RiskDefinition
  shipping_method: ShippingMethod!
  total_cubage_weigth: Int!
  updatedAt: String!
  weight: Int!
  width: Int!
}

type Mutation {
  createFreight(data: CreateFreightInput!): Freight!
  deleteFreightById(id: Int!): Boolean!
  updateFreightById(data: UpdateFreightInput!, id: Int!): Freight
}

type Query {
  findFreightById(id: Int!): Freight
}

# This define what is possible value of risk the freight
enum RiskDefinition {
  high
  low
  medium
}

# This defines the shipping model
enum ShippingMethod {
  aereo
  hidroviario
  rodoviario
}

# update a freight from id
input UpdateFreightInput {
  destination_city: String
  have_insurance: Boolean
  height: Int
  is_express: Boolean
  length: Int
  origin_city: String
  package_id: Int
  quantity: Int
  risk: RiskDefinition = low
  shipping_method: ShippingMethod
  weight: Int
  width: Int
}
